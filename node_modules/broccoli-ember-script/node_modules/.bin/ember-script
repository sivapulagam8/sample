#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ember-script/bin/ember-script" "$@"
  ret=$?
else 
  node  "$basedir/../ember-script/bin/ember-script" "$@"
  ret=$?
fi
exit $ret
